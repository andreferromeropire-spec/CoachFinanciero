DATABASE_URL="postgresql://postgres:postgres@localhost:5432/coach_financiero"
NEXTAUTH_SECRET="cambia-esto-por-un-secreto-seguro"
NEXTAUTH_URL="http://localhost:3000"
# URL del backend que usa el navegador/celular para fetch.
# En desarrollo desde PC: http://localhost:4000
# En desarrollo desde móvil en la misma red: http://TU_IP_LOCAL:4000
#   (buscar IP con: ip route get 1 | awk '{print $7}')
# En producción: URL pública del backend (Cloudflare Tunnel, Railway, etc.)
NEXT_PUBLIC_API_URL="http://localhost:4000"
PUBLIC_API_URL="https://coach-api.TUNDOMINIO.com"
PUBLIC_WEB_URL="https://coach.TUNDOMINIO.com"
