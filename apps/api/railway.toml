[build]
builder = "nixpacks"
buildCommand = "cd ../.. && npm install && cd packages/db && npx prisma generate && npx tsc && cd ../../apps/api && npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/api/health"
