[build]
builder = "nixpacks"
buildCommand = "npm install && cd packages/db && npx prisma generate && npx tsc && cd ../apps/api && npm run build"

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/api/health"
